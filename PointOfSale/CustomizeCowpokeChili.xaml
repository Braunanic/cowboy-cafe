<UserControl x:Class="PointOfSale.CustomizeCowpokeChili"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock FontSize="36">Cowpoke Chili Customization</TextBlock>
            <StackPanel Height="371">
                <Viewbox Height="50">
                    <CheckBox IsChecked="{Binding Path=GreenOnions}" HorizontalAlignment="Left">Green Onions</CheckBox>
                </Viewbox>
                <Viewbox Height="50">
                    <CheckBox IsChecked="{Binding Path=Cheese}" HorizontalAlignment="Left">Cheese</CheckBox>
                </Viewbox>
                <Viewbox Height="50">
                    <CheckBox IsChecked="{Binding Path=SourCream}" HorizontalAlignment="Left">Sour Cream</CheckBox>
                </Viewbox>
                <Viewbox Height="50">
                    <CheckBox IsChecked="{Binding Path=TortillaStrips}" HorizontalAlignment="Left">Tortilla Strips</CheckBox>
                </Viewbox>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
