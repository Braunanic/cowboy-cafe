<UserControl x:Class="PointOfSale.CustomizePecosPulledPork"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock FontSize="36">Pecos Pulled Pork Customization</TextBlock>
            <StackPanel Margin="50,50,50,0"  Height="400">
                <Viewbox Height="50">
                    <CheckBox IsChecked="{Binding Path=Bread}">Bread</CheckBox>
                </Viewbox>
                <Viewbox Height="50">
                    <CheckBox IsChecked="{Binding Path=Pickle}">Pickle</CheckBox>
                </Viewbox>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
